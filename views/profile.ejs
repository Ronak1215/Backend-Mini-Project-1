<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-between items-center">
        <h3 class="text-3xl"><span class="font-semibold">Hello,</span> <%= user.name %>👋</h3>
        <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
      <h5 class="mt-2">You can create a new post.</h5>
      <form action="/post" method="post">
        <textarea placeholder="What's on your mind ?" class="p-3 block outline-none w-full h-32 md:w-1/3  resize-none border-2 border-zinc-700 mt-7 rounded-md" name="content"></textarea>
        <input type="submit" class="px-3 text-sm py-2 bg-blue-500 rounded-md mt-4" value="Create post">
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Posts</h3>
        <div class="postcontainer mt-5">
          <% user.posts.reverse().forEach(function(post){ %>
            <div class="post w-full md:w-1/2 p-4 mb-4 rounded-md border-[1px] border-zinc-700 bg-zinc-800">
              <h4 class="text-blue-500">@<%= user.username %></h4>
              <p class="text-sm tracking-tight"><%= post.content %></p>
              <div class="btns flex gap-4 mt-5">
                <a class="text-blue-500" href="">Like</a>
                <a class="text-zinc-600" href="">Edit</a>
              </div>
            </div>
          <%})%>
        </div>
      </div>
    </div>
  </body>
</html>